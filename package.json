{
  "name": "adapt-scraper",
  "version": "1.0.0",
  "description": "A TypeScript-based Scraper for Insurance Data",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "verbose": "npm run build && node build/index.js -v",
    "test": "npm run build && tap \"build/tests/**/*.test.js\""
  },
  "devDependencies": {
    "@tapjs/typescript": "^3.0.0",
    "@types/cheerio": "^0.22.30",
    "tap": "^21.0.1",
    "ts-node": "^10.2.1",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "cheerio": "^1.0.0-rc.10"
  }
}
